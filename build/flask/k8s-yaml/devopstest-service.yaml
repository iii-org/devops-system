apiVersion: v1
kind: Service
metadata:
  name: devopstest-service
  labels: 
    app: devopstest
spec:
  type: NodePort
  selector:
    app: devopstest
  ports:
    - name: devopstest-port
      protocol: TCP
      port: 10009
